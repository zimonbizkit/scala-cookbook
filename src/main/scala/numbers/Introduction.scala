package numbers

import java.time.LocalDateTime

object Introduction {
  def main(args: Array[String]): Unit = {
    println(Short.MinValue)
    println(Short.MaxValue)
    println(Int.MinValue)
    println(Int.MaxValue)
    println(Float.MinValue)
    println(Float.MaxValue)


    // Check out Spire project for more complex number math classes

    // below needs the Joda Time

    //println(LocalDateTime.now)
    //println(LocalDateTime.now + 2.months)
  }
}
